# This is an locals.tf configuration file default values that can be overridden.
locals {
  default = {
    # cluster_name = "tiny-cluster"
    # cluster_zone = "europe-west1-b"
    # node_pool_name = "node-pool-single-small"
  }
}
